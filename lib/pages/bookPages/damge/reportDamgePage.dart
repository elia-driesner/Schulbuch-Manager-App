import 'package:flutter/material.dart';

class reportDamagePage extends StatelessWidget {
  const reportDamagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
